.claude/settings.local.json
*.log
.DS_Store

# Pipeline dataset cache — large, re-downloadable with `build_questions.py --fetch`
handoff/pipeline/cache/
__pycache__/
*.pyc

# Dev-only test deps (see handoff/package.json) — the game itself has none
node_modules/
